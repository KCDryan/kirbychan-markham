---
import AboutPage from '../../../views/AboutPage.astro';
import { OTHER_LANGS } from '../../../i18n';

export function getStaticPaths() {
  return OTHER_LANGS.map((lang) => ({ params: { lang } }));
}
---

<AboutPage />
